<%= form_for([@post, @post.comments.build]) do |form| %>
  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>

  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <br>

  <p>
    <%= form.submit %>
  </p>

<% end %>
